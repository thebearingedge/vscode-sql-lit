```js
const query = sql`select 1 as "one"`
```

```jsx
const query = sql`select 1 as "one"`
```

```ts
const query = sql<{ one: number }>`select 1 as "one"`
```

```tsx
const query = sql<{ one: number }>`select 1 as "one"`
```

```vue
<script>
  const query = sql`select 1 as "one"`
</script>
```
